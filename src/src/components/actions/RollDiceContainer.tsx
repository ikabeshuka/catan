import React from 'react';
import { DiceButton } from './DiceButton';

export const RollDiceContainer: React.FC = () => {
  return (
    <div className="w-full">
      <DiceButton />
    </div>
  );
};
