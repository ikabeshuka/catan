# 📌 מפת דרכים וארכיטקטורת המערכת (ROADMAP.md)

מסמך זה מהווה את **מקור האמת הארכיטקטוני (Source of Truth)** הרשמי עבור פרויקט משחק הקטאן הדיגיטלי (Catan Game), עם תמיכה מלאה בהרחבות **"יורדי הים" (Seafarers)** ו-**"סוחרים וברברים" (Merchants & Barbarians)**. מסמך זה נועד להבטיח שימור קונטקסט ומעקב מושלם אחר מבנה הקוד, הלוגיקה שלו ושלבי ההתקדמות של הפרויקט.

---

## 📂 1. סריקה ומיפוי של עץ הקבצים והתיקיות (`catan-game/src/src`)

להלן מבנה התיקיות והקובצי הליבה המרכזיים של המערכת כפי שהם קיימים בפועל:

```text
catan-game/src/src/
├── assets/                       # קבצי מדיה, תמונות וגופנים לוקאליים
│   └── react.svg
├── config/                       # קונפיגורציות ופריסות לוחות משחק
│   ├── seafarersBoardPreset.ts   # פריסות קבועות עבור הרחבת יורדי הים
│   ├── seafarersPresets.ts       # פריסות 3 ו-4 שחקנים עבור סנאריוז מגוונים
│   ├── standardVersion.ts        # הגדרות ומשאבים של גרסת הבסיס
│   └── starterBoardPreset.ts     # פריסת לוח התחלתית סטנדרטית
├── context/                      # ניהול מצב גלובלי (React Context)
│   ├── BoardContext.tsx          # ניהול פריסת הלוח, האריחים, הצמתים והצלעות
│   ├── GameContext.tsx           # ניהול שלבי המשחק, חוקים, מהלכים ולוגים
│   ├── GameUIContext.tsx         # ניהול מצבי ממשק המשתמש (אינטראקציה, פופאפים, עגלות)
│   ├── PlayerContext.tsx         # ניהול רשימת השחקנים, משאבים, קלפי פיתוח, מטבעות זהב ונקודות
│   └── UserContext.tsx           # ניהול פרטי המשתמש המקומי והעדפותיו
├── hooks/                        # לוגיקה ייעודית מופרדת (Custom Hooks)
│   ├── useAppTrade.ts            # ניהול הצעות ומסחר במערכת האפליקציה
│   ├── useAppTrophies.ts         # ניהול הישגים וגביעים
│   ├── useBoardInteraction.ts    # טיפול בלחיצות על קודקודים (כולל עגלות), צלעות ואריחים
│   ├── useBuild.ts               # ניהול פעולות בנייה (כבישים, יישובים, ערים, ספינות)
│   ├── useDice.ts                # ניהול זריקת הקוביות, אנימציות ותוצאות
│   ├── useTrade.ts               # ניהול ממשק המסחר בין שחקנים ועם הבנק
│   └── useTurnManager.ts         # ניהול מחזור התור, שלבי משנה, סבבי הקמה ותנועת עגלות
├── services/                     # שירותי תקשורת ואינטגרציה
│   ├── gameApi.ts                # ממשקי API של המשחק
│   ├── gameDispatcher.ts         # ניהול וניתוב פעולות משחק לוקאליות וסנכרון מול הרשת
│   ├── socket.ts                 # ניהול תקשורת WebSocket בזמן אמת
│   ├── tauriCommands.ts          # פקודות אינטגרציה עם Tauri
│   ├── gemini/                   # אינטגרציה למודל השפה של גוגל (Gemini LLM) לקבלת החלטות מהלכים
│   │   ├── boardSerializer.ts    # סיראליזציה של מצב הלוח והמשחק לקלט מותאם ל-Gemini
│   │   ├── geminiService.ts      # שירות קריאה ל-API של Gemini וניהול הפרומפטים
│   │   ├── GeminiSettingsModal.tsx # מודאל הגדרת מפתח API וסוג המודל עבור שחקני AI מבוססי Gemini
│   │   └── geminiTypes.ts        # הגדרות טיפוסים, ממשקים וקונפיגורציות ל-Gemini
│   └── network/                  # רכיבי תקשורת רשת בזמן אמת
│       └── socketService.ts      # שירות התחברות לשרת WebSocket (Render) וניהול שידורי פעולות בחדרי משחק
├── types/                        # טיפוסים והגדרות סוגים (TypeScript Typings)
│   ├── boardElements.types.ts    # טיפוסי צמתים (Vertices), צלעות (Edges) ומבנים
│   ├── game.types.ts             # טיפוסי שלבי משחק, סנאריוז ושלבי משנה
│   ├── gameActions.types.ts      # טיפוסי פעולות משחק (Game Actions) לסנכרון ומעקב מהלכים
│   ├── hex.types.ts              # טיפוסי קואורדינטות משושים ואריחים (Hex Tiles)
│   ├── player.types.ts           # טיפוסי שחקנים, בוטים, אסטרטגיות, מטבעות ומצבי עגלה
│   └── resources.types.ts        # הגדרות סוגי משאבים וקלפי משאבים
├── utils/                        # פונקציות עזר ומנועי חישוב מורכבים
│   ├── ai/                       # מערכת קבלת החלטות ומנוע של שחקני בוט (AI)
│   │   ├── aiController.ts       # ניהול קבלת ההחלטות הראשי של הבוט לפי שלב
│   │   ├── aiNames.ts            # שמות ומחולל שמות לבוטים
│   │   ├── getMediumBotTarget.ts # חישוב מטרות אופטימליות לבוטים ברמה בינונית
│   │   ├── decisionMakers/       # מנגנוני בחירה והחלטות של הבוט
│   │   │   ├── chooseBuildPhase.ts
│   │   │   ├── chooseRobberTarget.ts
│   │   │   ├── proposeTrade.ts
│   │   │   └── respondToTrade.ts
│   │   ├── evaluators/           # הערכת אריחים, צמתים ומהלכים אופטימליים לבוט
│   │   │   ├── aiTradeEvaluator.ts
│   │   │   ├── aiYieldEvaluator.ts
│   │   │   ├── evaluateEdges.ts
│   │   │   ├── evaluateNeeds.ts
│   │   │   └── evaluateVertices.ts
│   │   ├── helpers/              # עזרי קבלת החלטות לבוט
│   │   │   ├── devCardManager.ts
│   │   │   └── riskManager.ts
│   │   ├── phases/               # ניהול מהלכי בוט לפי שלבי המשחק
│   │   │   ├── robberPhase.ts
│   │   │   ├── setupPhase.ts
│   │   │   └── tradeAndBuildPhase.ts
│   │   └── strategies/           # אסטרטגיות משחק שונות של הבוטים
│   │       ├── balancedPortStrategy.ts
│   │       ├── cityDevStrategy.ts
│   │       ├── longestRoadStrategy.ts
│   │       └── types.ts
│   ├── array/                    # עזרי מערכים (כגון ערבוב חפיסות)
│   │   └── shuffleArray.ts
│   ├── gameEngine/               # מנוע חוקי המשחק הראשי
│   │   ├── checkLongestRoad.ts   # חישוב אורך הכבישים וזיהוי הדרך הארוכה ביותר
│   │   ├── checkWin.ts           # בדיקת תנאי ניצחון והשגת נקודות
│   │   ├── distributeInitialResources.ts # חלוקת משאבים ראשונית בשלב ההקמה השני
│   │   ├── distributeResources.ts # חלוקת משאבים שוטפת ופיצוי מטבעות זהב ב-M&B
│   │   ├── generateBoard.ts      # יצירת הלוח והאריחים על בסיס קונפיגורציה והרחבות
│   │   ├── generateEdges.ts      # יצירת הצלעות המחברות בין צמתים
│   │   ├── generateVertices.ts   # יצירת הקודקודים (צמתים) המשיקים לאריחים
│   │   ├── getOpenShipsForPlayer.ts # זיהוי ספינות פתוחות הניתנות להזזה
│   │   ├── moveRobber.ts         # העברת השודד וניהול חסימת אריחים
│   │   ├── robberSteal.ts        # ניהול גניבת קלפים משחקנים שכנים לשודד/שודד הים
│   │   ├── rollDice.ts           # חישוב תוצאת הטלת שתי קוביות
│   │   └── turnSnapshots.ts      # צילומי מצב של התור לשחזור מהלכים (Undo)
│   ├── hexMath/                  # מתמטיקה גיאומטרית של רשת משושים (Hex Grid)
│   │   ├── board3DMath.ts        # המרות גיאומטריות עבור עולם תלת-ממדי
│   │   ├── cubeToPixel.ts        # המרת קואורדינטות קובייה (q,r,s) לפיקסלים דו-ממדיים
│   │   ├── getHexDistance.ts     # חישוב מרחקים בין אריחים
│   │   ├── getHexNeighbors.ts    # מציאת אריחים שכנים
│   │   ├── getHexPointsString.ts # מחולל קודקודי משושה דו-ממדי
│   │   ├── getHexVertexCoordinates.ts # קבלת קואורדינטות קודקודים סביב אריח
│   │   ├── normalizeModel.ts     # פונקציות לנרמול, מירכוז ושינוי קנה מידה של מודלים תלת-ממדיים
│   │   ├── parseEdgeId.ts        # חילוץ קודקודים מזהה צלע
│   │   ├── parseVertexId.ts      # חילוץ אריחים מזהה קודקוד
│   │   └── tileGeometryEffects.ts# אפקטים ועיצובים גיאומטריים של אריחים
│   └── validation/               # מערכת חוקים ואישור מהלכי בנייה ומיקומים
│       ├── validateRoadPlacement.ts       # ולידציית בניית כביש רגיל
│       ├── validateSettlementPlacement.ts # ולידציית בניית יישוב (חוק המרחק והחיבור)
│       └── validateShipPlacement.ts       # ולידציית בניית ספינה (חוקי מים וקווי ספנות)
└── components/                   # רכיבי הממשק הויזואליים (React Components)
    ├── actions/                  # פאנלים וכפתורי פעולות (זריקה, בנייה, מסחר, שדרוג)
    │   ├── ActionSidebar.tsx     # סרגל פעולות צידי מרכז (כולל תמיכה בפאנל עגלה)
    │   ├── BuildActionsPanel.tsx # פאנל שליטה על רכישות ובנייה
    │   ├── BuildMenu.tsx         # תפריט אפשרויות בנייה מהירה
    │   ├── DiceButton.tsx        # כפתור הטלת קוביות מעוצב
    │   ├── GoldTradePanel.tsx    # פאנל ייעודי להמרת זהב ממשבצות זהב
    │   ├── RollDiceContainer.tsx # מיכל הטלת קוביות ואנימציות
    │   └── WagonUpgradePanel.tsx # פאנל שליטה, הנעה ושדרוג של עגלת המסחר (M&B)
    ├── board/                    # רכיבי הלוח הויזואלי הדו-ממדי והתלת-ממדי
    │   ├── Clouds3D.tsx          # שכבת עננים תלת-ממדית דקורטיבית
    │   ├── EdgeLine.tsx          # צלע אינטראקטיבית לבניית כביש/ספינה
    │   ├── GameBoard.tsx         # רכיב הלוח הראשי הדו-ממדי
    │   ├── GameBoard3D.tsx       # רכיב תצוגת תלת-מימד מבוסס React Three Fiber (עם עגלות ושודדי ים)
    │   ├── HexTile.tsx           # אריח משושה דו-מימד (משאב, מספר, שודד, שודד ים)
    │   ├── HexTile3D.tsx         # מודל אריח תלת-ממדי עם תמיכה באריחים מיוחדים
    │   ├── NumberToken.tsx       # אסימון מספר דו-ממדי
    │   ├── NumberToken3D.tsx     # אסימון מספר תלת-ממדי
    │   ├── Road3D.tsx            # מודל תלת-מימד של כביש/ספינה
    │   ├── Structure3D.tsx       # מודל תלת-מימד של יישובים וערים
    │   ├── VertexNode.tsx        # קודקוד אינטראקטיבי (תמיכה בבחירת עגלות ובנייה)
    │   └── 3d/                   # מודלים מיוחדים בתלת-מימד
    │       ├── Birds3D.tsx       # מודל ציפורים תלת-ממדי דקורטיבי
    │       ├── Dolphin3D.tsx     # מודל דולפין תלת-ממדי דקורטיבי
    │       ├── Harbor3D.tsx      # דגם נמל תלת-ממדי
    │       ├── Pirate3D.tsx      # דגם שודד ים תלת-ממדי
    │       ├── Robber3D.tsx      # דגם שודד יבשתי תלת-ממדי
    │       ├── SheepGroup3D.tsx  # מודל עדר כבשים תלת-ממדי דקורטיבי
    │       └── Wagon3D.tsx       # דגם עגלת מסחר תלת-ממדית (M&B)
    ├── common/                   # רכיבים משותפים
    │   └── Icons.tsx             # ספריית אייקונים של המשחק
    ├── lobby/                    # מסכי הלובי ותהליך הגדרת המשחק
    │   ├── LobbyChat.tsx         # רכיב צ'אט מובנה עבור חדר ההמתנה (Lobby Chat)
    │   ├── LobbyScreen.tsx       # מסך הגדרה ראשי
    │   ├── types.ts              # טיפוסי נתוני הלובי
    │   ├── modals/
    │   │   ├── MapModal.tsx      # מודאל תצוגת מפה מקדימה
    │   │   └── OnlineRoomModal.tsx # מודאל יצירה והצטרפות לחדרי משחק מקוונים (Multiplayer Room)
    │   └── steps/                # שלבי ההגדרה בלובי
    │       ├── LobbyStep1_Theme.tsx      # בחירת ערכת נושא
    │       ├── LobbyStep2_Expansion.tsx  # בחירת הרחבות (בסיס, יורדי הים, סוחרים וברברים)
    │       ├── LobbyStep3_PlayerCount.tsx# הגדרת כמות שחקנים
    │       └── LobbyStep4_PlayersSetup.tsx# הגדרת צבעים ושמות (שחקנים ובוטים)
    ├── modals/                   # מודאלים אינטראקטיביים של המשחק
    │   ├── DiscardOverlay.tsx          # מודאל השלכת קלפים בחצי תור (קוביות 7)
    │   ├── GoldFieldSelectionModal.tsx # מודאל בחירת משאב ממכרה זהב
    │   ├── MonopolyModal.tsx           # מודאל שימוש בקלף מונופול
    │   ├── TrophyModal.tsx             # מודאל הצגת הישגים
    │   └── YearOfPlentyModal.tsx       # מודאל שימוש בקלף שנת שפע
    ├── notifications/            # לוגים והודעות מערכת
    │   ├── GameLog.tsx           # יומן אירועי המשחק המפורט
    │   └── PhaseGuide.tsx        # מדריך ויזואלי של שלב המשחק ותתי-השלבים הנוכחיים
    └── playerPanel/              # לוחות מידע ונתונים של השחקנים
        ├── DevelopmentCardsPanel.tsx # פאנל ניהול ושימוש בקלפי פיתוח
        ├── ResourceCard.tsx      # כרטיס משאב ויזואלי
        └── ResourceContainer.tsx # מיכל משאבי השחקן הנוכחי
```

---

## 🛠️ 2. פירוט ארכיטקטורה ולוגיקת ליבה (Core Logic Analysis)

להלן הסברים ממוקדים ומדויקים בעברית על קבצי הליבה המרכזיים שסומנו כעמודי התווך של המערכת:

### 🧩 `generateBoard.ts` (מייצר הלוחות הראשי)
* **תפקידו במערכת:** יצירת מערך אריחי המשושים (`HexTile[]`) שמרכיבים את לוח המשחק, בהתאם לקונפיגורציית המשחק וסוג הלוח המבוקש.
* **לוגיקה ומנגנון:**
  * מקבל פרמטרים כגון סוג המשחק (בסיס, יורדי הים, סוחרים וברברים), סוג הפריסה (אקראית `RANDOM` או מובנית `STARTER`), הסנאריו הפעיל ומספר השחקנים.
  * במקרה של הרחבת **"יורדי הים"**, הוא פונה לקובץ ה-presets, טוען את הפריסה הסטטית המתאימה ומערבב משאבים ואסימוני מספרים בצורה חכמה לפי אזורים מוגדרים.
  * במקרה של הרחבת **"סוחרים וברברים"**, הוא מחליף אריחים ספציפיים לאריחי משימה ייעודיים: מרכז הלוח הופך ל-`CASTLE` (טירה), אריח צפון-מערבי ל-`QUARRY` (מחצבה), ואריח דרום-מזרחי ל-`GLASSWORKS` (מפעל זכוכית).

### 📜 `seafarersPresets.ts` (פריסות מובנות ליורדי הים)
* **תפקידו במערכת:** משמש כמאגר נתונים סטטי המגדיר מראש את פריסות הלוח השונות עבור הרחבת יורדי הים.
* **לוגיקה ומנגנון:**
  * מייצר קבועים גיאומטריים מדויקים הכוללים את רשת הקואורדינטות (q, r, s) של המשושים, סוגי המשאבים (כולל אריחי מים `WATER`, אריחי ערפל `FOG`, ומכרות זהב `GOLD_FIELD`), מזהי האיים (`islandId`), אסימוני המספרים המשויכים אליהם ומיקומי הנמלים הייחודיים לכל סנאריו (Heading for New Shores, Four Islands, Fog Island).

### ⏳ `useTurnManager.ts` (מנהל מחזור התורות והשלבים)
* **תפקידו במערכת:** מנהל את המכונה המצבית (State Machine) של מחזורי התור והשלבים השונים במשחק.
* **לוגיקה ומנגנון:**
  * עוקב אחר המעבר בין שלבי משחק ראשיים (`SETUP_ROUND_1`, `SETUP_ROUND_2`, `GAMEPLAY`, `GAME_OVER`) ותתי-שלבים (`BEFORE_ROLL`, `DISCARD_PHASE`, `ROBBER_PLACEMENT`, `TRADE_AND_BUILD`).
  * מנהל את סדר השחקנים ותורות הבוטים באופן מלא.
  * **אינטגרציה ל-"סוחרים וברברים":** מאתחל עגלות לכל השחקנים בצומת ברירת מחדל, מחשב ומאפס את נקודות התנועה של העגלה בכל תור בהתאם לרמתה (רמה 1: 4 נקודות, רמה 2: 5 נקודות, רמה 3: 6 נקודות), ומיישם לוגיקת גביית/חלוקת מטבעות זהב.

### 🔍 `validateSettlementPlacement.ts` (בקר חוקיות בניית יישובים)
* **תפקידו במערכת:** מהווה שומר הסף הקובע האם מיקום מסוים לבניית יישוב (צומת/קודקוד) הוא חוקי ומותר בהתאם לחוקי קטאן הרשמיים.
* **לוגיקה ומנגנון:**
  * **בדיקת ריקנות:** מוודא שעל הקודקוד המבוקש לא בנוי כבר שום יישוב או עיר.
  * **חוק המרחק (Distance Rule):** סורק את כל הצמתים השכנים המחוברים ישירות בצמתים משיקים ומוודא שהם ריקים לחלוטין (בנייה דורשת לפחות מרחק של שתי צלעות מכל יישוב קיים).
  * **חוק החיבור (Connection Rule):** מוודא שבמשחק הרגיל (מחוץ לשלבי ההקמה הראשוניים), לקודקוד המבוקש מחובר לפחות כביש אחד או ספינה אחת בבעלות השחקן הנוכחי.

---

## ⛵ 3. סטטוס משימות הרחבת "יורדי הים" (Seafarers)

להלן תמונת המצב המעודכנת של התקדמות פיתוח הרחבת יורדי הים:

### ✅ משימות שהושלמו בהצלחה (100% מוכנות)
- [x] **יישור המפות הסטטיות:** תיאום מושלם של הפריסות והקואורדינטות הגרפיות עבור כלל הסנאריוז הסטטיים של יורדי הים.
- [x] **תיקון מזהי האיים (Island IDs):** שיוך מדויק של כל אריח למזהה האי הייחודי שלו (`islandId`) לתמיכה בבונוסים על התיישבות באיים חדשים וחישובי שליטה.
- [x] **סנכרון הנמלים בצלעות ובקודקודים:** הבטחת סנכרון גיאומטרי ומבני מלא של מיקומי הנמלים על גבי הקודקודים (Vertices) וחיבורם בצורה נכונה לצלעות ולאריחי המשאב המקבילים.
- [x] **מיקום שודד הים מחוץ לגריד:** הגדרת קואורדינת בית התחלתית עבור שודד הים (השודד הימי שמסתובב על אריחי המים) מחוץ לרשת הלוח הפעיל עד להפעלתו הרשמית במשחק.
- [x] **מכניקת מכרה הזהב:** חלוקת משאבים מותאמת אישית; פתיחת מודאל `GoldFieldSelectionModal.tsx` המאפשר לשחקנים לבחור משאב עצמאי מהקופה, עם מנגנון קבלת החלטות מהיר לבוטים (AI).
- [x] **לוגיקת קווי ספנות מתקדמים:**
  * הגדרת חוקיות לבניית ספינות (על גבי צלעות מים בלבד, או צלעות חוף) באמצעות `validateShipPlacement.ts`.
  * הבחנה בין "קו ספינות פתוח" ל-"קו ספינות סגור" בעזרת `getOpenShipsForPlayer.ts`.
  * הטמעת הזזת הספינה הפתוחה האחרונה פעם אחת בתור בשלב המסחר והבנייה.
- [x] **חישוב הדרך הארוכה ביותר המשולבת:** שדרוג אלגוריתם מציאת המסלול הארוך ביותר ב-`checkLongestRoad.ts` כך שיחבר בצורה חלקה ורציפה בין כבישים וספינות של אותו שחקן, כל עוד החיבור מתבצע דרך יישוב או עיר שלו בנקודת המפגש שבין היבשה לים.
- [x] **מכניקת שודד הים (Pirate Mechanic):**
  * חסימת בניית ספינות חדשות על צלעות הגובלות בשודד הים (`hasPirate === true`).
  * מניעת הזזת ספינות פתוחות הממוקמות על צלעות הגובלות בשודד הים.
  * לוגיקת שדידה ימית ואוטומציה מלאה עבור שחקנים ובוטים.
- [x] **קלף הפיתוח 'בניית 2 דרכים' (חוק ה'):** התאמה המאפשרת בניית 2 דרכים, 2 ספינות, או דרך אחת וספינה אחת בחינם.

---

## 🚚 4. סטטוס משימות הרחבת "סוחרים וברברים" (Merchants & Barbarians)

להלן תמונת המצב המעודכנת של התקדמות פיתוח הרחבת סוחרים וברברים:

### ✅ משימות שהושלמו בהצלחה (100% מוכנות)
- [x] **מערכת מטבעות זהב (Gold Coins):** הטמעת מנגנון חלוקת מטבעות זהב ב-`PlayerContext.tsx` ו-`useTurnManager.ts` כפיצוי על תורים חלשים שבהם הטלת הקוביות אינה מניבה משאבים לשחקן.
- [x] **משימות הובלה ושיירות (Wagon & Convoys):**
  * בניית מנגנון תנועת עגלות על גבי קודקודי הלוח (`wagonPosition`) ומעקב אחר נקודות תנועה שנותרו (`remainingMovementPoints`).
  * יישום חישוב עלות תנועה לאורך צלעות (עלות בסיסית: 2 נקודות, עלות על כביש בבעלות השחקן: 1 נקודה, כביש של יריב: 1 נקודה + תשלום מטבע זהב ליריב כדמי מעבר).
- [x] **שדרוג עגלת המסחר:** יצירת פאנל `WagonUpgradePanel.tsx` המאפשר שדרוג של העגלה מרמה 1 עד רמה 3 באמצעות משאבים (1 עץ + 1 ברזל) או באמצעות 3 מטבעות זהב. כל שדרוג מגדיל את נקודות התנועה המקסימליות (רמה 1: 4 נקודות, רמה 2: 5 נקודות, רמה 3: 6 נקודות).
- [x] **אריחי הרחבה מיוחדים:** תמיכה בטעינת אריחי `CASTLE` (טירה), `QUARRY` (מחצבה), ו-`GLASSWORKS` (מפעל זכוכית) במרכז ובפינות הלוח ב-`generateBoard.ts` ובמנוע התצוגה.
- [x] **אינטגרציה תלת-ממדית (3D Representation):** יצירת המודל `Wagon3D.tsx` המציג את מיקומי העגלות של השחקנים השונים ישירות על גבי קודקודי לוח התלת-מימד ב-`GameBoard3D.tsx`, ואינדיקטור זוהר התומך בלחיצה והנעה ויזואלית חלקה של העגלה במצב תנועה פעיל.
- [x] **לוגיקת AI ותמיכה מלאה בבוטים:** התאמת הבוטים (AI) לקבלת החלטות חכמה ב-M&B (ניהול אסטרטגיות, מסחר, ושדרוגים).

---

## 🤖 5. שילוב רשת (Multiplayer) ובינה מלאכותית (Gemini AI Bot)

מערכת מולטיפלייר מלאה ואינטגרציה מתקדמת עם מודלי שפה (LLMs) להפעלת שחקנים עצמאיים חכמים במיוחד:

### ✅ משימות שהושלמו בהצלחה (100% מוכנות)
- [x] **תשתית חדרים מקוונים:** בניית `OnlineRoomModal.tsx` המאפשר ליצור חדרים, להצטרף לחדר קיים עם שם קסטום, ולשחק יחד עם שחקנים אמיתיים מרוחקים.
- [x] **חיבור זמן אמת (WebSockets):** פיתוח `socketService.ts` המקשר בין שחקנים לשרת תקשורת ייעודי המארח את חדר המשחק בזמן אמת.
- [x] **מערכת צ'אט בזמן אמת בלובי:** הטמעת רכיב `LobbyChat.tsx` וסנכרונו מול שרת ה-WebSockets, המאפשר לשחקנים הממתינים בחדר משחק מקוון להתכתב ביניהם בצ'אט קבוצתי אינטראקטיבי עם תצוגת שמות צבעונית לפי בחירת השחקן.
- [x] **מודל פעולות ריכוזי (Action Dispatcher):** הטמעת ארכיטקטורת `gameDispatcher.ts` המרכזת את כלל מהלכי המשחק לתוך טיפוס אחיד `GameAction`, ומנתבת אותם לביצוע מקומי ושידור לכל חברי החדר במקביל.
- [x] **אינטגרציה לבינה מלאכותית (Gemini LLM):** שילוב מנוע החלטות של מודל השפה `gemini-2.5-flash` של גוגל באמצעות `geminiService.ts`:
  * **סיראליזציה של המצב:** קובץ `boardSerializer.ts` הממיר את כל נתוני המפה, השחקנים והמשאבים לתוך אובייקט JSON קומפקטי.
  * **פרומפטינג מנוהל:** פרומפט מערכת חכם המנחה את הבוט לחתור לניצחון (10-12 נקודות) תוך התחשבות בחוקי הבסיס ויורדי הים (כמו חשיפת אריחי ערפל).
  * **קונפיגורציה קלה:** מודאל `GeminiSettingsModal.tsx` להגדרת מפתח ה-API, סוג המודל והפעלה/כיבוי של שחקני ה-AI מבוססי ה-Gemini.
  * **הסברים בעברית:** הבוט מחזיר את שיקול דעתו בעברית (`reasoningInHebrew`), המוצג ישירות בלוג ההודעות של המשחק לשאר השחקנים.

---

## 📈 הנחיות לעדכון ותחזוקה
קובץ זה מוגדר כ**מקור האמת הארכיטקטוני (Architecture Source of Truth)**. 
בכל פעם שמבוצעים שינויים במבנה הקבצים, בלוגיקות הליבה או בהתקדמות המשימות של הרחבות המשחק, יש לעדכן קובץ זה בהתאם כדי לשמור על מעקב וסנכרון מושלמים של צוות הפיתוח.
